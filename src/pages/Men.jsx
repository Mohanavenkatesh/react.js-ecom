import React from 'react'

export const Men = ({ array, setarray, theme, toggleTheme }) => {
  return (
    <div className={theme === 'light' ? 'bg-white text-black' : 'bg-black text-white'}>
     
    </div>
  )
}
